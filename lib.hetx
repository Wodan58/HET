/*
    module  : lib.hetx
    version : 1.4
    date    : 11/18/19
*/
( true t : ;
  false f : ;
* !) put-bool : ;

(a : ; b : ; a * b *) swap : ;

(swap * ! less $) greater : ;

t true : ;
f false : ;

(a : ; b : ; () a * + b * + > tt =) and : ;

(a : ; b : ; () a * + b * + > ff = f =) or : ;

(t = f =) not : ;

(list *
  () t : ;
  (list * / prog * ! step * !) f : ;
() = * !) step : ;

(list : ;			# old list
  ()				# new list
  (swap * ! ; +) prog : ;	# program
step * !) reverse : ;
